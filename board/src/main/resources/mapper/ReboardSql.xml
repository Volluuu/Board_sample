<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.ReboardMapper">
    <select id="getReboardCount"  resultType="int">
       	select count(*)
   		from "reboard"
    </select>
    
    <select id="reboardByUser" parameterType="int" resultType="ReboardDto">
    	
    </select>
    
    <insert id="insertReboard" parameterType="ReboardDto">
        
    </insert>
    <!--    아이디 중복 체크-->
    <update id="updateReboard" parameterType="ReboardDto">
    	
    </update>
   
    <!--    회원 탈퇴-->
    <delete id="deleteReboard" parameterType="int">
       
    </delete>
</mapper>